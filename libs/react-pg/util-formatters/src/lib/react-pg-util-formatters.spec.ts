import { reactPgUtilFormatters } from './react-pg-util-formatters';

describe('reactPgUtilFormatters', () => {
  it('should work', () => {
    expect(reactPgUtilFormatters()).toEqual('react-pg-util-formatters');
  });
});
