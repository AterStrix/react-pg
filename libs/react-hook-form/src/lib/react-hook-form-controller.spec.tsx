import { render } from '@testing-library/react';

import ReactHookFormController from './react-hook-form-controller';

describe('ReactHookFormController', () => {
  it('should render successfully', () => {
    // const { baseElement } = render(<ReactHookFormController />);
    // expect(baseElement).toBeTruthy();
  });
});
